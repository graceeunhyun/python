t = int(input())
for i in range(t):
    w = list(input())
    print(w)
    k = int(input())

    # 어떤 문자를 정확히 k 개를 포함하는 가장 짧은 연속 문자열의 길이를 구한다
    # 1. hash map 에 저장한다.
    # 2. 가장 짧은 것을 구하려면
    for elem in arr:


    # 어떤 문자열을 정확히 k 개를 포함하고,, 문자열의 첫번째와 마지막 글자가 해당 문자로 가장 긴 연속 문자열의 길이를 구한다.
    